import java.util.*;

public class PersonStats
{
	public static int getAverage(ArrayList<Person> persons)
		{
			int average = 0;
		
			for(Person p: persons)
				average += p.getAge();
			
			return average / persons.size();
		}
	
	public static string mostPets(ArrayList<Person> persons)
		{
			
			for(Person p: persons)
			{
				Person highest = p;
				// wip
				if(
			}
			
		}
}
